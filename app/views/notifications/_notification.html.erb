<% visiter = notification.visiter %>

<div>
  <%= link_to user_path(visiter) do %>
    <%= attachment_image_tag visiter, :profile_image, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50" %>
    <%= visiter.name %>
  <% end %>
  
  さんが <%= notification_form(notification) %>
  <span><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>

</div>
